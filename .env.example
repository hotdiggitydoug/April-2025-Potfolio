# Portfolio password gate (required in production on Vercel)
# Use the password you share with hiring managers.
PORTFOLIO_SITE_PASSWORD=

# Long random secret to sign the session cookie (e.g. run: openssl rand -hex 32)
PORTFOLIO_GATE_SECRET=
